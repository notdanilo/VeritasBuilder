FUNCTION(VERITAS_SET_ARCHITECTURE_PROPERTIES TARGET PARAMETER)
    IF(${CMAKE_SYSTEM_PROCESSOR} STREQUAL "i686")
        SET(PROCESSOR "x86")
    ELSE()
        SET(PROCESSOR "Unknown")
    ENDIF()
ENDFUNCTION()
